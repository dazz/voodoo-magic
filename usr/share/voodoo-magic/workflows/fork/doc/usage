Usage: fork NAME [ -- -x WORKFLOW , --target DIRECTORY , --product BRAND ]

Available options:
    -x              - exclude COMMAND from fork
    --target        - target-directory for the new fork
    --product       - product/brand of the new fork
