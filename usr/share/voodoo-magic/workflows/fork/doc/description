The FORK workflow allows you to fork the framework into a new project, e.g.
copy the entire framework code-base to a new directory root, while renaming all
directory- and filenames, as well es replacing all occurrences of product names
inline with the new fork's project name.
